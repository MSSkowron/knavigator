# kubectl

kubectl delete node $(kubectl get nodes --no-headers | awk '/^n/ {print $1}')

# Volcano

kubectl delete hypernodes.topology.volcano.sh --all && kubectl delete jobs.batch.volcano.sh --all

kubectl get job.batch.volcano.sh
